import { toast } from "react-toastify";
import { useState, useEffect ,useRef} from "react";

export function Dashboard() {
    return (
        <div className="w-full flex h-200 p-1 items-center justify-center">
            <h1 className="text-2xl text-center  text-slate-600 font-semibold">Dashboard</h1>
        </div>
    );
}

export default Dashboard